syntax = "proto3";

import "AuthMessage.proto";

option go_package = "../pbs;rpcApi";

service AuthService {
  rpc AnalysisToken (AnalysisTokenReq) returns (AnalysisTokenRsp) {}
  rpc CheckPermission (CheckPermissionReq) returns (CheckPermissionRsp) {}
}
